# Vanilla JavaScript Guidance

Use small scripts scoped to the artifact.

Useful primitives: tabs, `details` accordions, copy buttons with textarea fallback, sliders that update CSS variables, native drag/drop, and SVG detail panels keyed by `data-*`.

Prefer `textContent` for plain text and escape user-controlled strings before using `innerHTML`.
