# shadcn/ui Guidance

Use shadcn as an interaction and component-quality reference, not as a React dependency by default.

Borrow clear button variants, labeled inputs, validation messages, tabs, accordions, focused panels, visible focus states, and toast-like copy/export feedback.
