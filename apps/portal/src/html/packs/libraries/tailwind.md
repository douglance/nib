# Tailwind Guidance

Use Tailwind as a design mindset, not necessarily as a runtime dependency.

For standalone HTML, prefer handwritten CSS that mirrors utility values. Use CSS variables, grid/flex, gap, small radii, muted borders, and media queries. Avoid a Tailwind build step unless the user explicitly wants one.
