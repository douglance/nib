# Editor Artifact

Use for one-off editing interfaces: triage boards, feature flags, prompt tuning, data curation, annotation, or config review.

The editor should be the primary view. Include summary/warnings/diff/preview, reset, and copy/export. Preserve data order unless reordering is the task.
