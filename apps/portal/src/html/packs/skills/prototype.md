# Prototype Artifact

Use for visual directions, component variants, animations, and interaction tuning.

Put the prototype surface first, controls next to the tuned object, live readouts near controls, and copy/export for final parameters.
