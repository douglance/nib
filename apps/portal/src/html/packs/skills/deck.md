# Deck Artifact

Use for lightweight slide decks that still live as one HTML file.

Use one full-viewport section per slide, keyboard navigation, a slide counter, and print-friendly styles where useful.
